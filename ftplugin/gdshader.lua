-- Documentación: módulo `ftplugin/gdshader.lua`.
-- Propósito: ajustes locales para buffers gdshader sin afectar otros filetypes.

require('godot.ftplugin').setup({
  commentstring = '// %s',
  desc_prefix = 'Godot',
})
