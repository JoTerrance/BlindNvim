-- Documentación: módulo `lua/tools/mini-config/init.lua`.
-- Propósito: define utilidades y herramientas de edición dentro de BlindNvim sin alterar lógica de ejecución.

-- Línea documentada: `require('mini.map').setup()`.
require('mini.map').setup()
-- Línea documentada: `require('mini.cursorword').setup({})`.
require('mini.cursorword').setup({})
