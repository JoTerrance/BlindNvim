-- Documentación: módulo `lua/tools/orphans-config/init.lua`.
-- Propósito: define utilidades y herramientas de edición dentro de BlindNvim sin alterar lógica de ejecución.

-- Configuration for orphans.nvim
-- Plugin for managing orphaned buffers
-- Línea documentada: `require('orphans').setup({`.
require('orphans').setup({
  -- Configuration options for orphans.nvim
-- Línea documentada: `})`.
})
