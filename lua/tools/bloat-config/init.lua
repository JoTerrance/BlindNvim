-- Documentación: módulo `lua/tools/bloat-config/init.lua`.
-- Propósito: define utilidades y herramientas de edición dentro de BlindNvim sin alterar lógica de ejecución.

-- Configuration for bloat.nvim
-- Plugin for analyzing startup time and plugin impact
-- Línea documentada: `require('bloat').setup({`.
require('bloat').setup({
  -- Configuration options for bloat.nvim
-- Línea documentada: `})`.
})
